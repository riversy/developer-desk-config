---
- name: This playbook will setup all necessary environment for Magento development
  hosts: all

  vars_files:
    - role_vars/magento.yml

  pre_tasks:
    - name: Update apt cache if needed.
      apt: update_cache=yes cache_valid_time=86400

  roles:
    
